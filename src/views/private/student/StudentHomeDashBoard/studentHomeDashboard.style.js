import { Typography } from "@material-ui/core";
import styled from "styled-components";

export const StudentHomeDashboard = styled.div``;

export const Title = styled(Typography)`
  display: none;
`;
